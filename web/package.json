{
  "name": "sfp-leaderboard",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "echo 'Deploy via GH Actions self-hosted runner'"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
